[Service]
# Additional heartbeat and AI usage-log paths. This drop-in does not change
# ExecStart, so the current Testnet/Mainnet venue and CLI flags remain intact.
RuntimeDirectory=mybot
RuntimeDirectoryMode=0700
ReadWritePaths=/home/bot/apps/binance_bot/db /home/bot/apps/binance_bot/logs /run/mybot
